<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ThiGK">

        <!-- Màn hình chính -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Màn hình tính BMI -->
        <activity android:name=".BmiActivity" />

        <!-- Màn hình danh sách món ăn -->
        <activity android:name=".MonAnActivity" />

        <!-- Màn hình danh sách bài thuốc -->
        <activity android:name=".BaiThuocActivity" />

        <!-- Màn hình giới thiệu -->
        <activity android:name=".GioiThieuActivity" />

        <!-- Màn hình làm thêm - VN Express RSS -->
        <activity android:name=".LamThemActivity" />

    </application>

</manifest>